<div class="flex-row position-relative trail">
    <img src="{{ asset("build/images/WP_20170924_12_16_46_Rich.jpg") }}" class="back" alt="trail">
    <div class="row position-absolute justify-content-center">
        <div class="row trititle justify-content-center mb-2">
            <h2>Trail</h2>
        </div>
        <div class="row">
            <div class="col-lg-6 col-sm-12 cont-logo text-center">
                <img src="{{ asset('build/images/GTB-Blanc@2x.png') }}" class="logo" alt="logo gtb">
            </div>
            <div class="col-lg-6 col-sm-12 text-center">
                {% if trailHome is defined %}
                <p>{{ association.trailHome|raw }}</p>
                <a href="{{ path('trail') }}" class="btn btn-primary">Le trail</a>
                {% else %}
                <p>La section est en cours de création</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>