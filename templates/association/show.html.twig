{% extends 'base.html.twig' %}

{% block title %}Association{% endblock %}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ association.id }}</td>
            </tr>
            <tr>
                <th>TrailContent</th>
                <td>{{ association.trailContent }}</td>
            </tr>
            <tr>
                <th>TrailHome</th>
                <td>{{ association.trailHome }}</td>
            </tr>
            <tr>
                <th>TriathlonContent</th>
                <td>{{ association.triathlonContent }}</td>
            </tr>
            <tr>
                <th>TriathlonHome</th>
                <td>{{ association.triathlonHome }}</td>
            </tr>
            <tr>
                <th>ClubContent</th>
                <td>{{ association.clubContent }}</td>
            </tr>
            <tr>
                <th>ClubHome</th>
                <td>{{ association.clubHome }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('association_index') }}">back to list</a>

    <a href="{{ path('association_edit', {'id': association.id}) }}">edit</a>

    {{ include('association/_delete_form.html.twig') }}
{% endblock %}
