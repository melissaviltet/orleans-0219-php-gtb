{% extends 'base.html.twig' %}

{% block title %}Le trail - GTB{% endblock %}

{% block body %}
    {% include '_navbar.html.twig' %}
    {{ include ('home/_jumbotron.html.twig') }}
    <section class="sportpage justify-content-center">
        <div class="container">
            <h1 class="text-center">LE TRAIL</h1>
            <div class="text">
                {% if association.trailcontent is defined %}
                {{ association.trailcontent|raw }}
                {% else %}
                <p class="text-center">La page est en cours de construction</p>
                {%  endif %}
            </div>
        </div>
    </section>
    {% include '_footer.html.twig' %}
{% endblock %}
